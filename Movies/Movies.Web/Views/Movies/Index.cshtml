@model IEnumerable<Movies.Web.Models.Movie>

@{
    ViewData["Title"] = "Filmler";
}

<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <title>Movies DB Portal</title>
    <link rel="stylesheet" href="~/css/MoviesIndex.css">

</head>

<body>
    <nav class="top-menu">
        <a href="/">🏠 Anasayfa</a>
        <a href="/Movies">🎞 Filmler</a>
        <a href="/Actors">🎭 Oyuncular</a>
        <a href="/Report">🎬 Film Raporları</a>
    </nav>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2 class="text-primary"><i class="bi bi-film"></i> Film Listesi</h2>
            <a asp-action="Create" class="btn btn-success">
                <i class="bi bi-plus-circle"></i> Yeni Film Ekle
            </a>
        </div>

        <div class="card shadow-sm">
            <div class="card-body">
                @if (!Model.Any())
                {
                    <p>Henüz film eklenmemiş.</p>
                }
                else
                {
                    <table class="table table-hover table-striped align-middle">
                        <thead class="table-dark">
                            <tr>
                                <th>Ad</th>
                                <th>Tür</th>
                                <th>Yıl</th>
                                <th style="width: 250px;">İşlemler</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var movie in Model)
                            {
                                <tr>
                                    <td>@movie.Ad</td>
                                    <td>@movie.Tur</td>
                                    <td>@movie.Yil</td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@movie.MovieId" class="btn btn-sm btn-primary">
                                            Detaylar
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@movie.MovieId"
                                            class="btn btn-sm btn-warning text-white">
                                            Düzenle
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@movie.MovieId" class="btn btn-sm btn-danger">
                                            Sil
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>
        </div>
    </div>
</body>

</html>